language: go

go_import_path: dinowernli.me/almanac

addons:
  apt:
    packages:
      - protobuf-compiler

install:
  - tools/fetch-deps.sh
  - dep ensure

script:
  - tools/lint.sh
  - go vet ./...
  - tools/genproto.sh --check
  - tools/gendata.sh --check
  - go build ./...
  - go test ./...

